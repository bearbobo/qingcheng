# 轻程 部署说明

这是一个纯静态站点（HTML/CSS/JS），不需要构建步骤，数据库用的是 Supabase（项目名 `qingcheng`）。按下面步骤操作大概 10-15 分钟。

## 1. 建 GitHub 仓库并推送代码

```bash
cd qingcheng-site
git init
git add .
git commit -m "轻程 v1"
gh repo create qingcheng --public --source=. --push
# 如果没装 gh cli，就去 github.com 手动新建一个仓库，
# 然后 git remote add origin <你的仓库地址> 再 git push -u origin main
```

## 2. 打开 GitHub Pages

进入仓库 → Settings → Pages：
- Source 选 `Deploy from a branch`
- Branch 选 `main`，目录选 `/ (root)`
- 保存

等一两分钟，GitHub 会给你一个 `https://<你的用户名>.github.io/qingcheng/` 的地址，先用这个地址确认网站能正常打开、能注册登录。

## 3. 绑定 qingcheng.boorbit.com

**在 GitHub 这边**（仓库里已经有一个 `CNAME` 文件，内容是 `qingcheng.boorbit.com`，所以这一步 GitHub Pages 会自动识别，不用你手动填）：
- 还是在 Settings → Pages，"Custom domain" 那一栏应该会自动显示 `qingcheng.boorbit.com`
- 等 DNS 生效后，勾选 "Enforce HTTPS"（强制 HTTPS，务必勾上，因为要处理账号密码）

**在你的域名服务商那边**（管理 boorbit.com 的地方，比如 Namecheap / GoDaddy / Cloudflare）：
加一条 DNS 记录：

| 类型 | 主机名 | 值 |
|---|---|---|
| CNAME | qingcheng | `<你的GitHub用户名>.github.io` |

保存后一般几分钟到几小时生效（DNS 传播时间不固定）。生效后访问 `https://qingcheng.boorbit.com` 就能看到网站了。

## 4. Supabase 那边要不要再设置一下

数据库、权限规则我已经建好了，代码里也已经接好了项目地址和公开密钥（`supabase-config.js`，这个 key 是设计给前端公开用的，不是密码，安全性靠数据库的行级权限规则保证，不用藏起来）。

有一件事建议你去 Supabase 后台看一下：**Authentication → Providers → Email**，里面有个"Confirm email"（邮箱确认）开关。
- 开着：新用户注册后要点确认邮件里的链接才能登录，更安全，但要求 Supabase 项目配好发信（免费额度每小时发信数量有限，团队人少的话完全够用）。
- 关掉：注册后直接就能登录，对一个熟人小团队来说更省事。

看你们团队人数和使用场景，自己决定要不要关。

## 之后要加人怎么办

不用你手动加，谁都可以自己在网站上点"加入团队"注册账号、设昵称头像，注册那一刻数据库会自动帮 Ta 建好私人记录和团队墙的位置。

## 文件说明

- `index.html` / `style.css` / `app.js` — 网站本体
- `supabase-config.js` — 数据库连接信息（可以公开，不是密钥）
- `CNAME` — 告诉 GitHub Pages 绑定哪个自定义域名
